<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is blocked by default on Android 9+. The dev server runs on
  the host machine over plain HTTP, reached via 10.0.2.2 from an emulator and
  via localhost from a tethered debugger. Allow those two hosts only; every
  other destination still requires HTTPS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
